{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "M/OS v0.1a desktop capability",
  "windows": ["main", "quick-capture"],
  "permissions": [
    "core:default",
    "dialog:allow-open",
    "dialog:allow-save"
  ]
}
