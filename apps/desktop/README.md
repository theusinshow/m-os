# M/OS Desktop

Aplicativo Windows desktop-first do M/OS. A v0.1a implementa captura local, Inbox,
busca, arquivo, lixeira e backup/restore sobre Tauri, React e SQLite.

```powershell
npm install
npm run tauri dev
```

O banco local e os backups automáticos ficam no diretório de dados do aplicativo.
