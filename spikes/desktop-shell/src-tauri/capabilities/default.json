{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimum shell capability for the disposable desktop spike",
  "windows": ["main", "quick-capture"],
  "permissions": [
    "core:default"
  ]
}
